<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.chang888.system.mapper.MenuMapper">

    <select id="findAllByOpen" resultType="java.lang.Long">
        select id from tb_menu where open = 1
    </select>

    <select id="findMenuPermsByRoleId" resultType="top.chang888.common.entity.Menu" parameterType="long">
        select m.perms
        from tb_menu m , tb_role_menu rm
        where m.id = rm.menu_id and rm.role_id = #{id,jdbcType=BIGINT}
    </select>

</mapper>
